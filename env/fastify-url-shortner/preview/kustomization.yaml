apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: fastify-url-shortner-preview

resources:
- deployment.yaml

images:
- name: fastify-url-shortner
  newName: nginx
  newTag: latest
