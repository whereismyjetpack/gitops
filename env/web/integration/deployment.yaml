---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
  labels:
    app: fastify-url-shortner
spec:
  replicas: 19
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: fastify-url-shortner
        image: web:latest
        ports:
        - containerPort: 80