apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: fastify-url-shortner
    foo: bar
    thing: true
  name: nginx-deployment
  namespace: int
spec:
  replicas: 19
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - image: 774331681746.dkr.ecr.us-west-2.amazonaws.com/pro-monorepo-api:vc7a8234be646ca94d675344ca5023560cbcd2462
        name: main
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: fastify-url-shortner
  namespace: int
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: fastify-url-shortner
  type: ClusterIP
